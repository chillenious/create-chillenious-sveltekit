{
  "name": "@{{PROJECT_NAME}}/datastore",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "tsc -w",
    "lint": "eslint . --ext .ts,.tsx",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "test": "bun test",
    "migrate": "bun src/migrate.ts",
    "migrate:test": "bun src/migrate-test.ts"
  },
  "dependencies": {
    "@{{PROJECT_NAME}}/logger": "workspace:*",
    "kysely": "^0.27.3",
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "@types/pg": "^8.11.6",
    "typescript": "^5.8.3",
    "eslint": "^9.27.0",
    "kysely-migrate": "^0.0.16"
  }
}
